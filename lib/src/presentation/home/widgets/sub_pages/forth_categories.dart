import 'package:flutter/material.dart';

class ForthCategories extends StatelessWidget {
  const ForthCategories({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(child: Text("Beauty Category", style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold))),

      ],
    );
  }
}
