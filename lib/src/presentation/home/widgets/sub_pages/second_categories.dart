import 'package:flutter/material.dart';

class SecondCategories extends StatelessWidget {
  const SecondCategories({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(child: Text("Men Category", style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold))),

      ],
    );
  }
}
